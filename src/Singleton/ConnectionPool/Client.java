package Singleton.ConnectionPool;

import javax.sql.DataSource;
import java.awt.image.DataBufferShort;
import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;

//import static Singleton.ConnectionPool.ConnectionPoolImpl.getInstance;

public class Client {
    public static void main(String[] args) {
//        ConnectionPoolImpl connectionPool = ConnectionPoolImpl.getInstance(10);
//        ExecutorService executorService = Executors.newCachedThreadPool();
//        connectionPool.initializePool();
//        connectionPool.initializePool();
//        connectionPool.initializePool();
//        connectionPool.initializePool();
//        connectionPool.initializePool();
//        connectionPool.initializePool();
//        connectionPool.initializePool();
//        DatabaseConnection c = connectionPool.getConnection();
//        DatabaseConnection c2 = connectionPool.getConnection();
//        System.out.println(connectionPool.getAvailableConnectionsCount() +" "+ connectionPool.getTotalConnectionsCount());
//        connectionPool.releaseConnection(c);
//        connectionPool.releaseConnection(c2);
//
//        System.out.println("after releasing "+connectionPool.getAvailableConnectionsCount() +" "+ connectionPool.getTotalConnectionsCount());
////        System.out.println(connectionPool.getTotalConnectionsCount());
////        for(int i=0;i<100;i++){
//////            executorService(connectionPool.initializePool());
////        }

    }
}
